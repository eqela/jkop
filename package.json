{
	"name": "@eqela/jkop",
	"scripts": {
		"build-node": "npx -y @eqela/slingc@3.12.0 build-node src -tmpdir .tmp -output dist/node -npm-install",
		"build-browser": "npx -y @eqela/slingc@3.12.0 build-browser src -tmpdir .tmp -output dist/browser",
		"clean": "rm -rf .tmp dist"
	}
}
